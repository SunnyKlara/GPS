# GPS测速仪 —— 项目完成度评估与下一步操作指南

> 本文档是项目推进的**核心行动手册**  
> 请按照文档中的步骤逐一执行，每完成一步打勾 ☑  
> 2026-03-26

---

## 一、项目完成度总览

### 1.1 进度条

```
已完成 ██████████████░░░░░░ 待完成
         70%                30%
```

### 1.2 各阶段状态

| 阶段 | 内容 | 状态 | 完成度 |
|------|------|------|--------|
| Phase 1 | PC模拟环境 + NMEA解析 + 速度计算 + 里程 | ✅ 完成 | 100% |
| Phase 2 | BLE HID模拟 + 按键 + GUI模拟器 + 单元测试 | ✅ 完成 | 100% |
| 文档 | 架构分析 + SDK分析 + 开发指南(4份文档) | ✅ 完成 | 100% |
| **Phase 3** | **SDK接入 + HAL真机实现** | **❌ 未开始** | **0%** |
| **Phase 4** | **硬件联调 + 户外实测** | **❌ 未开始** | **0%** |

### 1.3 已完成的具体成果

```
源代码(13个文件):
  ✅ app/app_main.c/h        主应用调度(113行)
  ✅ app/app_speed.c/h        测速+里程+滤波(105行)
  ✅ app/app_display.c/h      数码管显示管理(132行)
  ✅ app/app_ble_hid.c/h      BLE HID音乐控制(77行)
  ✅ app/app_key.c/h           按键消抖+长按(74行)
  ✅ lib/nmea_parser.c/h      NMEA协议解析器(213行)
  ✅ config/config.h           全局配置(53行)
  ✅ platform/hal.h            HAL接口定义(54行)
  ✅ platform/platform_sim/hal_sim.c     PC模拟HAL(225行)
  ✅ platform/platform_sim/sim_main.c    CLI模拟器(218行)
  ✅ platform/platform_sim/sim_gui.c     GUI模拟器(483行)

测试(3个文件, 9个用例):
  ✅ tests/test_main.c         测试入口
  ✅ tests/test_nmea.c         NMEA解析测试(4用例)
  ✅ tests/test_speed.c        速度计算测试(5用例)

构建脚本(3个文件):
  ✅ Makefile / build.bat / build.ps1

文档(5份):
  ✅ README.md                 项目说明
  ✅ DEVELOPMENT_GUIDE_PART1.md 开发文档上篇(架构分析)
  ✅ DEVELOPMENT_GUIDE_PART2.md 开发文档下篇(SDK接入指南)
  ✅ SDK_ANALYSIS.md           SDK资料深度分析
  ✅ NEXT_STEPS_GUIDE.md       本文档(行动指南)
```

---

## 二、剩余工作分类

### 2.1 需要你(用户)操作的 🔴

这些是我无法替代完成的，**必须由你亲自操作**：

| # | 操作 | 为什么需要你 | 预计耗时 |
|---|------|-------------|---------|
| U1 | 下载杰理SDK到本地 | 需要在你电脑上执行git clone | 5分钟 |
| U2 | 安装杰理编译工具 | 需要在你电脑上安装软件 | 15分钟 |
| U3 | 提供PCB原理图(引脚分配) | 这是硬件设计信息，我没有 | - |
| U4 | 确认芯片型号 | 确认AC6323A对应SDK哪个系列 | 5分钟 |
| U5 | 编译SDK示例验证工具链 | 在你电脑上操作 | 10分钟 |
| U6 | 烧录固件到开发板 | 需要物理硬件操作 | - |
| U7 | 硬件联调测试 | 需要物理硬件操作 | - |

### 2.2 我(AI)可以完成的 🟢

给我必要信息后，我可以立刻完成：

| # | 工作 | 需要的前置信息 | 预计耗时 |
|---|------|--------------|---------|
| A1 | 修改HID Report为SDK位域格式 | 无(现在就能做) | 10分钟 |
| A2 | 编写hal_ac6323a.c真机HAL层 | 需要U1+U3 | 2小时 |
| A3 | 编写SDK工程集成代码 | 需要U1 | 1小时 |
| A4 | 配置board引脚 | 需要U3 | 30分钟 |
| A5 | 编写烧录和测试流程文档 | 需要U1 | 30分钟 |

---

## 三、逐步操作指南（请按顺序执行）

---

### 第一步：下载杰理SDK 📥

**目标**：把SDK源码下载到你的电脑上

**操作**：
```powershell
# 打开PowerShell，执行以下命令：
cd D:\Users\26054\Desktop\GPS
git clone https://github.com/Jieli-Tech/fw-AC63_BT_SDK.git
```

如果GitHub太慢，用Gitee镜像：
```powershell
git clone https://gitee.com/Jieli-Tech/fw-AC63_BT_SDK.git
```

**预期结果**：
```
D:\Users\26054\Desktop\GPS\
├── gps_speedometer/        # 我们的项目
└── fw-AC63_BT_SDK/         # 杰理SDK ← 新增
    ├── apps/
    │   └── hid/            # HID应用（我们要用的）
    ├── include_lib/
    ├── libs/
    └── ...
```

**完成后告诉我**：
> "SDK已下载到 D:\Users\26054\Desktop\GPS\fw-AC63_BT_SDK"

☐ 已完成

---

### 第二步：安装杰理编译工具 🔧

**目标**：安装SDK所需的编译器

**操作**：
1. 打开浏览器访问：https://doc.zh-jieli.com/Tools/zh-cn/dev_tools/dev_env/index.html
2. 按页面说明下载并安装编译工具
3. 如果页面需要注册/登录，先注册杰理开发者账号

**备选方案**：
- SDK目录内 `tools/` 文件夹可能自带编译器
- 下载完SDK后先检查 `fw-AC63_BT_SDK/tools/` 目录

**完成后告诉我**：
> "编译工具已安装"

☐ 已完成

---

### 第三步：确认芯片型号 🔍

**目标**：确认AC6323A在SDK中对应的系列和板级

**操作**：
1. 查看你手上的芯片/开发板上的丝印标记
2. 在SDK的 `apps/hid/board/` 目录下查看有哪些子目录：

```powershell
dir D:\Users\26054\Desktop\GPS\fw-AC63_BT_SDK\apps\hid\board\
```

3. 常见对应关系：
   - AC632N系列 → `bd19` 目录
   - AC631N系列 → `bd29` 目录
   - AC636N系列 → `bd47` 目录

4. AC6323A**大概率**属于AC632N系列（`bd19`），但请确认

**完成后告诉我**：
> "芯片是AC632N系列，对应bd19" 或 "芯片丝印是 xxx，board目录有 xxx"

☐ 已完成

---

### 第四步：编译SDK自带HID示例 🏗️

**目标**：验证编译工具链能正常工作

**操作（CodeBlocks方式）**：
1. 安装 CodeBlocks IDE（如果没有的话）
2. 打开工程文件：
```
fw-AC63_BT_SDK/apps/hid/board/bd19/AC632N_hid.cbp
```
3. 按 `Ctrl+F9` 编译
4. 看是否编译成功，生成固件文件

**操作（Makefile方式）**：
1. 双击 `fw-AC63_BT_SDK/tools/make_prompt.bat`
2. 输入对应的make target（参考Makefile开头注释）

**完成后告诉我**：
> "编译成功，生成了 xxx.bin 固件" 或 "编译出错，错误信息是 xxx"

☐ 已完成

---

### 第五步：提供PCB原理图 📋

**目标**：获取引脚分配信息

**我需要知道以下引脚对应AC6323A的哪个IO口**：

```
┌─────────────────────────────────────────────┐
│ 请告诉我以下各信号连接的具体引脚号             │
│                                              │
│ 1. GPS模块 UART:                             │
│    AT6558A TX → AC6323A 的哪个 RX 引脚？      │
│    例如：PA1 / PA2 / PB0 ...                  │
│                                              │
│ 2. LED驱动 I2C:                              │
│    TM5020A SCL → AC6323A 的哪个引脚？         │
│    TM5020A SDA → AC6323A 的哪个引脚？         │
│                                              │
│ 3. 5个按键:                                   │
│    按键0(播放/暂停) → AC6323A 的哪个引脚？    │
│    按键1(上一曲)    → AC6323A 的哪个引脚？    │
│    按键2(下一曲)    → AC6323A 的哪个引脚？    │
│    按键3(音量+)     → AC6323A 的哪个引脚？    │
│    按键4(音量-)     → AC6323A 的哪个引脚？    │
│                                              │
│ 4. 调试串口(如果有):                          │
│    DEBUG TX → AC6323A 的哪个引脚？            │
└─────────────────────────────────────────────┘
```

**如何获取这些信息**：
- 方法A：查看PCB原理图（.sch / .pdf文件），找到AC6323A芯片连线
- 方法B：如果有现成的板子，看板上丝印标注
- 方法C：如果PCB还没设计，你来定义引脚分配，告诉我即可
- 方法D：把原理图文件/截图直接发给我，我来分析

**完成后告诉我**：
> "GPS RX=PA1, I2C SCL=PA2, SDA=PA3, 按键0=PA4 ..." 这样的格式

☐ 已完成

---

### 第六步：交给我继续开发 🤖

**当你完成了第一步~第五步后，告诉我以下信息**：

```
1. SDK路径：D:\...\fw-AC63_BT_SDK
2. 芯片系列：AC632N / bd19 (或其他)
3. 编译结果：成功 / 失败(附错误信息)
4. 引脚分配：
   GPS UART RX = PAx
   I2C SCL = PAx
   I2C SDA = PAx
   KEY0 = PAx
   KEY1 = PAx
   KEY2 = PAx
   KEY3 = PAx
   KEY4 = PAx
   DEBUG TX = PAx (如果有)
```

**我拿到这些信息后，会立刻完成**：
- ✍️ 创建 `hal_ac6323a.c` 真机HAL层（调用SDK API）
- ✍️ 修改 `app_ble_hid.c` 适配SDK的HID位域格式
- ✍️ 编写SDK工程集成代码
- ✍️ 配置板级引脚文件
- ✍️ 生成完整的编译和烧录指南

---

### 第七步：烧录测试 🔥

**你需要操作**：
1. 连接USB烧录器到开发板
2. 芯片进入下载模式
3. 使用烧录工具下载固件
4. 连接调试串口（波特率 **1000000 bps**）
5. 上电观察串口日志

**逐模块验证顺序**：
```
☐ 7.1  串口有日志输出
☐ 7.2  串口打印出GPS NMEA原始数据($GNRMC...)
☐ 7.3  数码管能亮（显示固定数字测试）
☐ 7.4  按键能触发事件（串口打印按键ID）
☐ 7.5  数码管显示GPS速度
☐ 7.6  里程掉电保存（断电重启后里程不清零）
☐ 7.7  手机蓝牙搜索到 "GPS-Speedometer"
☐ 7.8  手机连接后，按键能控制音乐播放
☐ 7.9  户外测试GPS定位和速度准确性
☐ 7.10 连续运行8小时稳定性测试
```

**遇到问题时**：
- 把串口日志截图/复制给我
- 把错误现象描述给我
- 我会分析原因并修改代码

---

## 四、时间线预估

```
第一步~第五步（你操作）: 约 1-3 小时（取决于网速和原理图获取）
第六步（我开发）:         约 2-3 小时
第七步（你测试+我修BUG）: 约 3-5 天

总计从现在到项目完成: 约 1-2 周
```

---

## 五、风险提示

### 高风险项
1. **BLE HID兼容性** — 可能需要反复调试Report描述符才能兼容iOS/Android
   - 应对：我已分析了SDK自带的描述符，可以直接复用
2. **杰理SDK学习曲线** — 闭源部分（lib.a）无法看源码
   - 应对：SDK有完整示例+官方文档+钉钉技术群(107855006323)

### 中风险项
3. **I2C通信** — TM5020A可能需要调时序
   - 应对：准备了GPIO模拟I2C备用方案
4. **Flash寿命** — 30秒写一次可能超出寿命
   - 应对：改用SDK的NV存储API

### 低风险项
5. **NMEA解析/速度计算/按键逻辑** — 纯算法已在PC充分测试
   - 真机上应该可以直接工作

---

## 六、文件清单速查

| 文件 | 用途 |
|------|------|
| `README.md` | 项目概述 |
| `DEVELOPMENT_GUIDE_PART1.md` | 开发文档上篇：架构+模块详解 |
| `DEVELOPMENT_GUIDE_PART2.md` | 开发文档下篇：HAL实现+联调+踩坑 |
| `SDK_ANALYSIS.md` | SDK资料分析+HID描述符对比+集成方案 |
| `NEXT_STEPS_GUIDE.md` | **本文档：行动指南（按步骤执行）** |
| `参考资料网址` | 所有参考链接汇总 |

---

## 七、快速沟通模板

当你完成操作后，可以直接复制以下模板填写发给我：

```
【进度更新】
已完成步骤: 第 ___ 步
SDK路径: ___
芯片系列: ___
编译结果: 成功 / 失败
引脚分配:
  GPS UART RX = ___
  I2C SCL = ___
  I2C SDA = ___
  KEY0 = ___
  KEY1 = ___
  KEY2 = ___
  KEY3 = ___
  KEY4 = ___
  DEBUG TX = ___
遇到的问题: ___
```

---

*本文档是项目推进的行动手册，请保存并按步骤执行。任何步骤遇到问题随时问我。*
